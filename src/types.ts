export interface navbar {
  title: string;
  url: string;
}
export interface AstroURL {
  pathname: string;
}

export interface Astro {
  url: AstroURL;
}

export interface foot {
  titel: string;
  url: string;
}
